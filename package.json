{
  "name": "machinist-3",
  "license": "Apache-2.0",
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 test",
    "test:setup": "pnpm --filter machinist test:setup",
    "test:unit": "pnpm -r --workspace-concurrency=1 test:unit",
    "test:e2e:ecs": "pnpm --filter machinist test:e2e:ecs",
    "test:teardown": "pnpm --filter machinist test:teardown",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "cleanall": "rm pnpm-lock.yaml && rm -rf node_modules && rm -rf services/*/node_modules",
    "clean": "rm -rf node_modules && rm -rf services/*/node_modules",
    "start": "wattpm start",
    "dev": "wattpm dev"
  },
  "dependencies": {
    "platformatic": "^3.54.0",
    "wattpm": "^3.54.0"
  },
  "devDependencies": {
    "snazzy": "^9.0.0",
    "standard": "^17.1.2"
  },
  "version": "2.4.0"
}
