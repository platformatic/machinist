---
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-echo-server-replicaset
  labels:
    app.kubernetes.io/instance: rs-fixture
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: rs-fixture
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: rs-fixture
    spec:
      containers:
        - name: nginx-echo-server
          image: platformatic/machinist-test:latest
          imagePullPolicy: Always
          ports:
            - name: app
              containerPort: 8080
              protocol: TCP
          resources:
            requests:
              memory: 512Mi
              cpu: 500m
            limits:
              memory: 1Gi
              cpu: 1000m
